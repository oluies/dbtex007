name: nightly · mssql ext leak check

# Runs Valgrind memcheck against upstream HEAD's duckdb-cli + mssql
# extension while it copies our DDD tables across the ATTACH boundary.
# Same artifact pair as the daily smoke canary (ABI-matched by
# construction), but executed under valgrind --leak-check=full so we
# get a leak summary per allocation site.
#
# The job stays green as long as the *infrastructure* ran — the leak
# numbers themselves are informational, surfaced via the job summary
# and the uploaded log artifact. DuckDB intentionally retains some
# allocations until process exit, so "still reachable" bytes are not
# bugs; watch "definitely lost" / "indirectly lost" for regressions.
#
# Manual-trigger only — run via the Actions UI or:
#   gh workflow run nightly-mssql-leak-check.yml --ref main
# Triggered also on PRs that touch this file so changes are verified
# before merging.

on:
  # Manual-only: valgrind runs are expensive (~60-min budget) and pull
  # from an external repo's artifacts, so we'd rather opt in explicitly
  # than burn minutes on every cron. Also kept on PRs that touch this
  # workflow file so changes are verified before merging.
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/nightly-mssql-leak-check.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read

jobs:
  leak-check:
    name: valgrind (upstream HEAD · duckdb-cli + mssql ext)
    runs-on: ubuntu-latest
    # Under valgrind each ATTACH→probe→DETACH iteration takes ~30s
    # (TLS handshake + TDS round-trip + 10–30× valgrind overhead).
    # 25 iterations × 30s ≈ 13 min of SQL, plus ~5 min for download +
    # apt-install valgrind + compose up. 60-min budget leaves headroom
    # for runner variance and one-off slow pulls.
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6

      - name: Install valgrind
        run: |
          sudo apt-get update -qq
          sudo apt-get install -yq --no-install-recommends valgrind
          valgrind --version

      - name: Materialize .env and export into job env
        run: |
          cp .env.example .env
          while IFS= read -r line; do
            [[ -z "$line" || "$line" =~ ^[[:space:]]*# ]] && continue
            key="${line%%=*}"
            val="${line#*=}"
            val="${val#\'}"; val="${val%\'}"
            val="${val#\"}"; val="${val%\"}"
            printf '%s=%s\n' "$key" "$val" >> "$GITHUB_ENV"
          done < .env

      - name: Download upstream duckdb-cli + extension (same CI run)
        id: ext
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/upstream
          read -r run_id run_sha run_url < <(
            gh api 'repos/hugr-lab/mssql-extension/actions/runs?branch=main&status=success&per_page=20' \
              --jq '[.workflow_runs[] | select(.name == "CI")] | .[0] | "\(.id) \(.head_sha[0:7]) \(.html_url)"'
          )
          echo "Upstream CI run: $run_url (sha $run_sha)"
          gh run download "$run_id" --repo hugr-lab/mssql-extension \
            --name duckdb-cli-linux_amd64      --dir /tmp/upstream/duckdb-cli
          gh run download "$run_id" --repo hugr-lab/mssql-extension \
            --name mssql-extension-linux_amd64 --dir /tmp/upstream/ext
          duckdb_bin=$(find /tmp/upstream/duckdb-cli -type f -name 'duckdb' -print -quit)
          ext_path=$(find /tmp/upstream/ext -name '*.duckdb_extension' -print -quit)
          chmod +x "$duckdb_bin"
          echo "DUCKDB_BIN=$duckdb_bin"      >> "$GITHUB_ENV"
          echo "MSSQL_EXT_PATH=$ext_path"    >> "$GITHUB_ENV"
          echo "upstream_sha=$run_sha"       >> "$GITHUB_OUTPUT"
          echo "upstream_run_url=$run_url"   >> "$GITHUB_OUTPUT"

      - name: Start SQL Server containers
        run: docker compose up -d --wait

      - name: Wait for source seed to complete
        run: |
          set -euo pipefail
          for i in {1..30}; do
            count=$(docker exec ddd-mssql-source /opt/mssql-tools18/bin/sqlcmd \
              -S localhost -U sa -P "$MSSQL_SA_PASSWORD" -C -No -d ddd_source -h-1 -W \
              -Q "SET NOCOUNT ON; SELECT COUNT(*) FROM ddd.locations" 2>/dev/null \
              | tr -d '[:space:]' || true)
            count="${count:-0}"
            if [[ "$count" == "10" ]]; then
              echo "Source seeded ($count locations)."
              exit 0
            fi
            echo "  attempt $i: locations=$count, waiting..."
            sleep 2
          done
          echo "::error::Source seed did not complete in time"
          docker compose logs mssql-source | tail -100
          exit 1

      - name: Generate iterated ATTACH/DETACH SQL
        # Repeat the ATTACH → DETACH cycle LEAK_ITERATIONS times
        # (default 10). Each iteration exercises one src + one dst
        # ATTACH/DETACH pair = two full TLS + TDS connection
        # lifecycles. A leak of N bytes per attach amplifies to
        # ~20N bytes after the loop.
        #
        # Why only 10? Empirically, under valgrind each src+dst
        # ATTACH/DETACH pair runs at ~5 min (much slower than the
        # naive 10–30× valgrind-overhead estimate — TLS handshake
        # under memcheck is expensive). 10 iters × 5 min ≈ 50 min,
        # which fits the 60-min job budget with setup. Override via
        # the LEAK_ITERATIONS repository variable if a specific
        # regression needs more amplification.
        #
        # No probe SELECT here: the earlier valgrind trace already
        # showed ATTACH triggers the full TLS handshake (MSSQLAttach
        # → ValidateConnection → DoPrelogin → EnableTls), so the
        # connection lifecycle is exercised even without a query.
        # The dst probe also failed in the previous shape because
        # the mssql extension doesn't expose information_schema or
        # sys.tables in the attached-catalog namespace.
        env:
          LEAK_ITERATIONS: ${{ vars.LEAK_ITERATIONS || '10' }}
        run: |
          set -euo pipefail
          mkdir -p valgrind
          {
            printf -- '-- Generated by nightly-mssql-leak-check.yml\n'
            printf -- '-- ATTACH/DETACH iterations: %s\n' "${LEAK_ITERATIONS}"
            printf "LOAD '%s';\n" "${MSSQL_EXT_PATH}"
            for ((i=1; i<=LEAK_ITERATIONS; i++)); do
              printf -- '-- iter %d / %s\n' "$i" "${LEAK_ITERATIONS}"
              printf "ATTACH '%s' AS src (TYPE mssql, READ_ONLY);\n" "${SRC_MSSQL_DUCKDB_CONN}"
              printf "ATTACH '%s' AS dst (TYPE mssql);\n"            "${DST_MSSQL_DUCKDB_CONN}"
              printf 'DETACH src;\n'
              printf 'DETACH dst;\n'
            done
          } > valgrind/smoke.sql
          attach_count=$(grep -c '^ATTACH ' valgrind/smoke.sql)
          detach_count=$(grep -c '^DETACH ' valgrind/smoke.sql)
          echo "Generated valgrind/smoke.sql: ATTACH=$attach_count DETACH=$detach_count"
          echo "LEAK_ITERATIONS=${LEAK_ITERATIONS}" >> "$GITHUB_ENV"

      - name: Run iterated ATTACH/DETACH under valgrind
        # --error-exitcode=0 keeps valgrind from propagating findings as
        # a non-zero exit; we surface the report via summary/artifact.
        # |& tee captures both streams for diagnostics if duckdb itself
        # crashes (would not appear in --log-file).
        run: |
          set -uo pipefail
          # --track-origins=yes more than doubles runtime — dropped
          # for budget reasons; we still get full leak counts and
          # stack traces, just not "uninitialised values came from"
          # provenance, which we don't need for leak detection.
          # --show-leak-kinds=definite,indirect skips the noisy "still
          # reachable" / "possible" buckets in detail listings but
          # they're still counted in the LEAK SUMMARY.
          valgrind \
            --tool=memcheck \
            --leak-check=full \
            --show-leak-kinds=definite,indirect \
            --num-callers=20 \
            --error-exitcode=0 \
            --log-file=valgrind/memcheck.log \
            "$DUCKDB_BIN" -unsigned :memory: < valgrind/smoke.sql |& tee valgrind/duckdb-stdout.log
          duckdb_ec=${PIPESTATUS[0]}
          echo "duckdb-cli exit code: $duckdb_ec"
          if [[ -s valgrind/memcheck.log ]]; then
            echo "Valgrind log size: $(wc -c < valgrind/memcheck.log) bytes"
          else
            echo "::warning::Valgrind produced no log"
          fi

      - name: Summarize leak report
        if: always()
        env:
          UPSTREAM_SHA: ${{ steps.ext.outputs.upstream_sha }}
          UPSTREAM_RUN_URL: ${{ steps.ext.outputs.upstream_run_url }}
        run: |
          {
            echo "### Valgrind memcheck — upstream HEAD"
            echo
            echo "- Upstream commit: \`${UPSTREAM_SHA}\`"
            echo "- Upstream CI run: ${UPSTREAM_RUN_URL}"
            echo
            if [[ -s valgrind/memcheck.log ]]; then
              echo "#### HEAP / ERROR SUMMARY"
              echo
              echo '```'
              # Grab the standard valgrind summary tail: HEAP SUMMARY,
              # LEAK SUMMARY, ERROR SUMMARY blocks.
              awk '
                /HEAP SUMMARY:/ { in_block=1 }
                in_block { print }
                /ERROR SUMMARY:/ { print ""; exit }
              ' valgrind/memcheck.log | sed -E 's/^==[0-9]+== ?//'
              echo '```'
              echo
              echo "Full log: see the \`valgrind-memcheck\` workflow artifact (retained 30 days)."
            else
              echo "_No valgrind log produced — see the run logs for what went wrong._"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload valgrind log
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: valgrind-memcheck-${{ github.run_id }}
          path: |
            valgrind/memcheck.log
            valgrind/duckdb-stdout.log
            valgrind/smoke.sql
          retention-days: 30
          if-no-files-found: warn

      - name: Dump container logs on failure
        if: failure()
        run: |
          docker compose logs --tail=200 mssql-source || true
          docker compose logs --tail=200 mssql-dest || true
